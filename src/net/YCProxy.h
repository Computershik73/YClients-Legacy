#import <Foundation/Foundation.h>

/**
 * HTTP-прокси для отладки: посмотреть, что именно уходит на сервер.
 *
 * Нужно ровно для одного — поставить между приложением и api.yclients.com
 * перехватчик (Charles, Proxyman, mitmproxy, Fiddler) и увидеть запросы
 * целиком. Устройства, на которых это приложение живёт, отладчиком
 * не достаются, а журнал показывает только то, что мы сами решили в него
 * записать; когда сервер отвечает отказом без пояснения, этого мало.
 *
 * **Включённый прокси снимает проверку сертификата.** Иначе он бесполезен:
 * перехватчик подменяет сертификат своим, и честная проверка его отвергнет —
 * цепочка и правда чужая. Это делает соединение открытым для того, кто
 * стоит посередине, а ходят здесь логин с паролем. Поэтому:
 *
 *   * выключено по умолчанию;
 *   * в готовую сборку (FINALPACKAGE=1) не попадает вовсе — кода нет
 *     в бинарнике, и включить нечего;
 *   * пока включено, каждый запрос пишет об этом в журнал.
 *
 * Значения хранятся в NSUserDefaults и переживают перезапуск: перехватчик
 * обычно нужен не на один запрос, а на сеанс отладки.
 */
@interface YCProxy : NSObject

/**
 * Собрана ли поддержка прокси в этой сборке.
 *
 * NO в готовой сборке. Экран настроек по этому признаку показывает
 * объяснение вместо переключателя — иначе переключатель, который ничего
 * не делает, выглядел бы поломкой.
 */
+ (BOOL)isCompiledIn;

/**
 * Умеет ли эта система ходить через прокси — теперь всегда да.
 *
 * Осталось от прежней реализации на NSURLSession, которая требовала iOS 7.
 * Со своим TLS туннель CONNECT пробивается своими силами, и версия системы
 * к делу не относится. Метод оставлен, чтобы экран настроек не пришлось
 * переписывать, если однажды появится случай, когда прокси недоступен.
 */
+ (BOOL)isSupported;

/** Включён ли прокси пользователем. */
+ (BOOL)isEnabled;
+ (void)setEnabled:(BOOL)enabled;

/** Адрес прокси. По умолчанию 192.168.1.183. */
+ (NSString *)host;
+ (void)setHost:(NSString *)host;

/** Порт прокси. По умолчанию 8886. */
+ (NSInteger)port;
+ (void)setPort:(NSInteger)port;

/**
 * Идут ли запросы через прокси **прямо сейчас**.
 *
 * Всё сразу: собрано, поддерживается системой, включено и адрес не пуст.
 * Именно это спрашивает YCHttp перед каждым запросом.
 */
+ (BOOL)isActive;

/** `192.168.1.183:8886` — для показа в строке настроек. */
+ (NSString *)address;

/** Короткое состояние для строки списка: адрес либо причина, почему нет. */
+ (NSString *)summary;

@end
