#import <UIKit/UIKit.h>

/**
 * Настройка прокси для отладки.
 *
 * Один экран на два входа — с экрана входа и из меню календаря. Отдельным
 * контроллером, а не двумя разделами в двух таблицах, именно поэтому:
 * переключатель, снимающий проверку сертификата, должен вести себя
 * одинаково, откуда бы его ни открыли, и жить в одном месте.
 */
@interface YCProxyController : UITableViewController
@end
