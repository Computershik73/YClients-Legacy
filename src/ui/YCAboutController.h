#import <UIKit/UIKit.h>

/**
 * О программе: кто сделал, куда писать, чем поддержать — и журнал.
 *
 * Журнал здесь не для полноты. Приложение работает на устройствах, до
 * которых не дотянуться отладчиком, и единственный способ узнать, что
 * пошло не так, — забрать `Documents/yclients.log`. Путь к нему показан
 * прямо на экране, потому что иначе его пришлось бы диктовать по телефону.
 */
@interface YCAboutController : UITableViewController
@end
