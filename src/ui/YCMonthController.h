#import <UIKit/UIKit.h>

/**
 * Календарь на месяц: сетка чисел, нажатие переводит журнал на этот день.
 *
 * Полоса недели внизу журнала хороша для соседних дней и бесполезна, когда
 * надо на три недели вперёд: до них приходится долистывать по семь дней
 * за раз, теряя счёт. Здесь месяц виден целиком, и до любого дня —
 * одно нажатие.
 *
 * Месяцы листаются вверх-вниз одной лентой, а не по одному с кнопками
 * «назад» и «вперёд»: конец сентября и начало октября — соседние дни,
 * и разделять их страницами значило бы ломать то, что человек и так
 * держит в голове.
 */
@interface YCMonthController : UIViewController

/**
 * Показывает месяц выбранного дня и отмечает его.
 *
 * onChoose зовётся с полуночью выбранного дня; экран закрывается сам —
 * решать это вызывающему незачем, других исходов у выбора даты нет.
 */
- (id)initWithDay:(NSDate *)day onChoose:(void (^)(NSDate *chosen))onChoose;

@end
