#import <UIKit/UIKit.h>

/**
 * «Оформление»: как в системе · светлая · тёмная.
 *
 * Выбор применяется сразу — корневой экран пересобирается, и этот
 * список исчезает вместе с ним; так задумано: показывать «до и после»
 * на одном экране было бы нечем.
 */
@interface YCAppearanceController : UITableViewController
@end
