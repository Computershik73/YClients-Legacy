#import <UIKit/UIKit.h>

@class YCClient;

/**
 * Выбор клиента из базы филиала.
 *
 * Подсказки, которые всплывают под полями формы, появляются только после
 * трёх набранных знаков — и о них надо догадаться. Кнопка «Выбрать
 * из базы» ведёт сюда, где список открыт сразу и виден целиком: это тот
 * же поиск, но заявленный вслух.
 *
 * Экран только выбирает и ничего не меняет: правка карточки клиента —
 * дело веб-интерфейса, а здесь нужен ответ на вопрос «кто из них».
 */
@interface YCClientPickerController : UITableViewController

/**
 * Блок зовётся с выбранным клиентом; экран закрывается сам.
 *
 * При отказе от выбора — нажали «назад» — блок не зовётся вовсе:
 * ничего не выбрать это тоже ответ, и подставлять на этот случай
 * пустого клиента было бы хуже, чем не подставлять ничего.
 */
- (id)initWithQuery:(NSString *)query onChoose:(void (^)(YCClient *client))onChoose;

@end
