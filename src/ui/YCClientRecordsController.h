#import "YCFormScreen.h"

#import "YCRecordFormController.h"

@class YCClient;

/**
 * Все записи одного клиента: что делали, когда и с какой пометкой.
 *
 * Экран заведён ради телефонного разговора. Звонит человек, говорит «это
 * Ирина, я у вас была весной» — и за те несколько секунд, пока он говорит,
 * надо вспомнить, что ему делали, у кого и с какими пометками, а потом
 * записать его снова. Раньше для этого приходилось листать календарь
 * назад по дням, наугад.
 *
 * Поэтому здесь список сверху вниз от свежего к старому, в каждой строке
 * дата, услуги и комментарий, а внизу — кнопка «Записать снова».
 *
 * Попасть сюда можно с двух сторон: из списка клиентов и из самой записи
 * в календаре — в разговоре одинаково часто отталкиваются и от человека,
 * и от того, что уже стоит в расписании.
 */
@interface YCClientRecordsController : YCFormScreen

/**
 * Клиент известен номером и именем.
 *
 * Номер нужен серверу, имя — заголовку: ждать ответа, чтобы написать,
 * чья это карточка, незачем.
 */
- (id)initWithClientId:(NSInteger)clientId
                  name:(NSString *)name
                 phone:(NSString *)phone;

/**
 * Кому сообщать, что записи изменились.
 *
 * Тот же делегат, что у формы записи: с этого экрана заводят новую
 * запись, и календарь должен об этом узнать.
 */
@property (nonatomic, weak) id<YCRecordFormDelegate> delegate;

@end
