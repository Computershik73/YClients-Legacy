#import <UIKit/UIKit.h>

/** Главный экран: журнал записей на один день. */
@interface YCDayController : UIViewController

/** Показываемый день; по умолчанию сегодняшний. */
@property (nonatomic, strong) NSDate *day;

/**
 * Перейти к дню.
 *
 * Отличается от присваивания day тем, что перечитывает записи и двигает
 * сетку. Нужен календарю на месяц: выбранная там дата должна открыться,
 * а не просто записаться в поле.
 */
- (void)goToDay:(NSDate *)day;

/**
 * Перечитать сотрудников и записи текущего дня.
 *
 * Наружу вынесено ради возвращения в приложение: записи заводят и в
 * веб-интерфейсе, и по телефону, и день, показанный два часа назад,
 * к моменту возврата уже неверен.
 */
- (void)reloadAll;

@end
