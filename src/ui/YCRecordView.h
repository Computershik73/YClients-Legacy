#import <UIKit/UIKit.h>

#import "YCModel.h"

/**
 * Прямоугольник записи в сетке дня.
 *
 * Отдельным видом, а не отрисовкой внутри сетки, ровно ради перетаскивания:
 * поднять, подвинуть и вернуть на место — это три строчки, когда у записи
 * есть свой слой, и своя система координат с ручным перерисовыванием,
 * когда нет.
 */
@interface YCRecordView : UIView

/** Запись, которую этот вид показывает. */
@property (nonatomic, strong) YCRecord *record;

/**
 * Поднята ли запись пальцем.
 *
 * Поднятая рисуется с тенью и чуть крупнее — иначе, потащив запись,
 * не понять, взялась она или экран просто прокручивается.
 */
@property (nonatomic, assign) BOOL lifted;

@end
