#import <UIKit/UIKit.h>

/**
 * Экран «список плюс жёлтая кнопка снизу».
 *
 * Заведён после того, как кнопка уехала вместе со списком. Причина была
 * в UITableViewController: у него `self.view` — это **сама таблица**, и
 * кнопка, добавленная в неё, становится частью прокручиваемого содержимого.
 * В форме записи она к тому же лежала в tableFooterView, и по клавиатуре
 * список доскролливался в самый низ — на экране оставалась одна кнопка
 * и белое поле под ней.
 *
 * Здесь таблица и кнопка — соседи внутри обычного вида. Кнопка прибита
 * к низу, поднимается над клавиатурой и никуда не уезжает; список получает
 * снизу отступ ровно под неё, чтобы последняя строка не пряталась.
 *
 * Наследники задают стиль таблицы и заголовок кнопки; данные и делегат
 * таблицы — на них же.
 */
@interface YCFormScreen : UIViewController <UITableViewDataSource, UITableViewDelegate>

/** Стиль задаётся до загрузки вида; по умолчанию Plain. */
- (id)initWithStyle:(UITableViewStyle)style buttonTitle:(NSString *)buttonTitle;

@property (nonatomic, readonly) UITableView *tableView;

/** Жёлтая кнопка внизу; nil, если заголовок при создании был nil. */
@property (nonatomic, readonly) UIButton *button;

/**
 * Дополнительный вид над кнопкой — «Сбросить» в выборе сотрудника.
 * Ставится наследником; высота берётся из его кадра.
 */
@property (nonatomic, strong) UIView *accessoryView;

/** Нажали кнопку. Наследник переопределяет; по умолчанию ничего. */
- (void)buttonTapped;

@end
